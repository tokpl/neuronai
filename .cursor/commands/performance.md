# /performance

Investigate performance with Neuron context.

1. Call `neuron_context` with the slow path or scalability question
2. Prefer returned modules and files before profiling guesses
3. Verify hotspots in source; measure before optimizing
4. Do not claim gains without evidence
