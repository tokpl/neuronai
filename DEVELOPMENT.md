# Development

```bash
pnpm install
pnpm build
pnpm test
pnpm neuronai --help
```

- Monorepo root: `neuronai-monorepo` (private)
- Published CLI: `neuronai` in `apps/cli`
- Internal packages: `@neuronai/*`

Node 22+. No Docker / Postgres.
