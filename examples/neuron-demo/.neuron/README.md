# Neuron demo memories (optional seed for store.json — created by analyze)

This folder's markdown brain is enough for Cursor to read; run `neuron init` /
`neuron analyze` to populate `data/store.json` for MCP search.
