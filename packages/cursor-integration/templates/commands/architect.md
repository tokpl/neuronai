# /architect

Design systems with Neuron Architect Mode.

1. Call `neuron_run_mode` with `modeId: "architect"` (or query starting with `/architect`)
2. Optionally `neuron_mode_context` for required context
3. Follow suggested tools (`neuron_architecture_review`, `neuron_reason`, graph tools)
4. Return Summary / Evidence / Findings / Recommendations / Confidence

Do not auto-generate large code changes.
