# /performance

Performance engineer mode.

1. Call `neuron_run_mode` with `modeId: "performance"` (e.g. “why is this slow?”)
2. Share latency / DB / API symptoms
3. Use `neuron_performance_review`, `neuron_scalability_check`
4. Output Bottlenecks, Impact, Optimization plan
