# @neuron-ai-memory/project-analyzer

Detects project root metadata for Neuron:

- name / slug / deterministic `projectId`
- stack hints from manifests (`package.json`, `Cargo.toml`, …)
- git presence
